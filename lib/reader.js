const fs = require('fs');

function readThreeFiles(paths, callback) {
    let files = 
    //loop through paths first
    for (i = 0; i < paths.lengths; i++ ) {
        if callback(error) {
           console.log(error);
        } else { 
            callback(null, result);

        }
        
    }

}

fs(3, reader)

// var fs = require('fs');
// fs.readFile('data.txt', function (err, data) {
//   if (err)
//     throw err;
//   if (data)
//     console.log(data.toString('utf8'));
// });